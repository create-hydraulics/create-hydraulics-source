{
	"__comment": "Active variant. Same geometry as the inactive model (parented from the BlockBench export). Only one texture (hydrostatic_intake.png) was supplied, so the active state reuses it for now. To give the active state its own look: drop a texture at textures/block/hydrostatic_intake_active.png and change \"0\" below to createhydraulics:block/hydrostatic_intake_active.",
	"parent": "createhydraulics:hydrostatic_intake",
	"render_type": "minecraft:cutout",
	"textures": {
		"0": "createhydraulics:block/hydrostatic_intake"
	}
}
